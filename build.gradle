group 'me.fromgate.playeffect'
version '1.0-SNAPSHOT'

allprojects {
    apply plugin: 'java'
    sourceCompatibility = 1.8
    repositories {
        mavenCentral()
    }
}

subprojects.each { sp ->
    sp.buildDir = this.buildDir
}